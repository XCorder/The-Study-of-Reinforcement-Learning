The algorithm used here is partially from page 117(Incremental off-policy every-visit MC policy evaluation).
Different from the algorithm in folder "racetrack", I append the discount gamma and EpisodePerEvaluation here.
The result is bad, it seems that the algorithm is not good, so I changed the algorithm in folder "racetrack_3"